# PortableWhisper Backend Dependencies
# Phase 2: Parakeet TDT v3 + onnx-asr
# Compatible with Python 3.11 (PyInstaller) / 3.13 (dev)

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.6
websockets>=13.0

# Data validation
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Utilities
python-dotenv>=1.0.0

# Audio processing
sounddevice>=0.4.6
numpy>=1.26.0
scipy>=1.12.0
pyaudio>=0.2.14

# Speech-to-Text (Phase 2: Parakeet TDT v3 via onnx-asr)
onnx-asr>=0.11.0
onnxruntime>=1.20.0
huggingface-hub>=0.30.0
# Für GPU/NPU-Beschleunigung (Phase 2B):
# onnxruntime-directml>=1.20.0  # ersetzt onnxruntime