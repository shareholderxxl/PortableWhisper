# PortableWhisper Backend — Portable Build-Dependencies (Phase 2)
#
# Zweck: PyInstaller-Build des Backends in eine eigenstaendige
# whisper-backend.exe (Sidecar). onnx-asr + onnxruntime (OHNE torch,
# OHNE ctranslate2). Python 3.11 empfohlen (PyInstaller-Kompatibilitaet).
#
# Install:
#   python -m venv venv && venv\Scripts\activate
#   pip install -r requirements-portable.txt

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.6
websockets>=13.0

# Data validation
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Utilities
python-dotenv>=1.0.0

# Audio processing
sounddevice>=0.4.6
numpy>=1.26.0
scipy>=1.12.0
soundfile>=0.12.1
pyaudio>=0.2.14  # Alternative fuer Windows WASAPI

# Speech-to-Text (Phase 2 — Parakeet TDT v3 via onnx-asr, OHNE torch)
onnx-asr>=0.11.0
onnxruntime-directml>=1.20.0  # CPU + DirectML (GPU-Support für AMD/Intel/NVIDIA). Qwen3 Standard-Transformer ist DirectML-kompatibel.
huggingface_hub>=0.30.0  # fuer on-demand Modell-Download

# LLM-Post-Processing (Phase 3C — Qwen3-1.7B Korrektur via plain onnxruntime)
tokenizers>=0.21.0       # Tokenisierung des Korrektur-Modells (tokenizer.json)

# Build-Tooling
pyinstaller>=6.10.0,<7  # auf 6.x pinnen: collect_all liefert hier stabil 3 Werte
